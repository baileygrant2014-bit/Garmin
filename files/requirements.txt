# Pinned to exact versions on purpose (supply-chain safety): a future
# compromised release of any of these can't slip in silently during install.
# The package name is exactly "garminconnect" -- beware lookalikes.
#
# Garmin has no official API, so garminconnect drives the mobile login flow.
# If Garmin changes something and login breaks, bump this deliberately and
# re-run --login (don't leave it floating).
garminconnect==0.2.25
requests==2.32.3
